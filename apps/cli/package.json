{
  "name": "cli",
  "version": "0.0.0",
  "private": true,
  "main": "./index.ts",
  "types": "./index.ts",
  "dependencies": {
    "chalk": "^5.1.2",
    "commander": "^9.4.1"
  },
  "scripts": {
    "lint": "TIMING=1 eslint \"**/*.ts*\"",
    "build": "rm -rf dist && tsup index.ts --format=esm --dts --external react",
    "dev": "rm -rf dist && tsup index.ts --format=esm --watch --dts --external react"
  },
  "bin": {
    "supereffective-cli": "./dist/index.mjs"
  },
  "devDependencies": {
    "@types/node": "^18.11.9",
    "eslint": "^8.27.0",
    "eslint-config-custom": "workspace:^",
    "tsconfig": "workspace:^",
    "tsup": "^6.4.0",
    "typescript": "^4.8.4"
  }
}
